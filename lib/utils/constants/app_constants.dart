class AppConstants {
  // -- Login Constants * START * --
  static const greetingsPageIndex = 0;
  static const signInPageIndex = 1;
  static const signUpPageIndex = 2;
  // -- Login Constants * END * --
}